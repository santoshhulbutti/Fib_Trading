Date 02-May-2025

checklist
    In engine.py, you are using:
        calculate_trailing_sl(entry_price, price, TRAILING_RULES)
    👉 Ensure that function handles sorted keys properly

    In time_utils.py, You are using:
        is_eod_exit_time()
    👉 Ensure it correctly checks: time >= 15:20





Date 01-May-2025
FILES TO UPDATE FOR LIVE DEPLOYMENT (IMPORTANT)
When you switch from TEST → LIVE (options), you must update these:
    config/trading_params.py
        →  test_mode = False
        →  Parameter	        Change
           SL_POINTS	        2 → 25
           TRAILING_RULES	    equity → options

    main.py
        →  test_mode = False

    config/settings.py
        For live:
            Ensure env variables exist
            No hardcoded keys
            AWS-ready





Date - 30-Apr-2025
in main.py
    Initialize code block commented for initial test on Equity trade with 1 qty

in orders.py
    added stop sell order function block to short "place_stop_sell", (later commented this)
    added SL order function block for short position "place_sell_sl_order"
    added GET ORDER BY ID block
    added CHECK IF ORDER FILLED


in trading_params.py
    SL points changed
    trailing rules changed
    MAX_TRADES_PER_DAY changed
    MAX_DAILY_LOSS changed

in engine.py
    called place_stop_sell order to short during brakedown condition  (later commented this)
    to place SL order after buy order is filled the logic is updated in ENTRY LOGIC & CHECK ENTRY ORDER EXECUTION block


in state.py
    # Tracking order added
